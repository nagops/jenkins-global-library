#!/usr/bin/env groovy

def call(String name = 'human') {
  echo "We at OpsTree aim to be the earth’s most focused Ops company.
In doing so we wish to ensure that we allow all the creative minds to focus on creation., ${name}."
}

